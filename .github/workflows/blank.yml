# This is a basic workflow to help you get started with Actions

name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get public key
        run: gh api /repos/chancancode/test-gha/actions/secrets/public-key
